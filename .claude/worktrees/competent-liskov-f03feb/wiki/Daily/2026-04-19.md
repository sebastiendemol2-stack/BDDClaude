---
date: 2026-04-19
tags: [daily]
type: daily
status: active
---

# 2026-04-19

## Actions

- Création de `C:/Claude-Obsidian/index.js` — script Node.js connectant Claude API au vault via tool use (lecture + écriture)
- Configuration de `.env` (clé API, chemin vault) et résolution des problèmes de chargement dotenv/dotenvx
- Test du script validé : vault chargé, liaison API fonctionnelle, réponse en streaming
- `/ingest` exécuté : compilation de `raw/notes/exemple-article.md` → `wiki/Intelligence/context-engineering.md`
- Index et log mis à jour

## Décisions

- Architecture tool use agentique : Claude dispose de 4 outils (`vault_read`, `vault_list`, `vault_write`, `vault_append`) pour interagir dynamiquement avec le vault au lieu de tout charger en contexte statique
- Écriture bloquée sur `raw/` et `CLAUDE.md` au niveau du handler — les règles du CLAUDE.md sont enforced dans le code

## Prochaine étape

- Ajouter du contenu dans `raw/` (notes, articles, docs) pour enrichir le wiki via `/ingest`
- Créer des notes de contexte dans `wiki/Context/` (profil, projets en cours)

---

## Mise à jour — session 2 (2026-04-19)

### Actions

- Architecture multi-vault implémentée dans `C:/Claude-Obsidian/index.js`
- Ajout de `PROJECT_VAULT_PATH` dans `.env` → `C:/Cowork-App/docs/vault`
- Système de préfixes `kb:` / `project:` pour tous les outils (`vault_read`, `vault_write`, etc.)
- Protection raw/ et CLAUDE.md appliquée dans les deux vaults
- Test `/prime` validé : lecture des deux index en une seule session, synthèse croisée

### Décisions

- Les chemins d'outils sont préfixés (`kb:wiki/...` vs `project:wiki/...`) pour que Claude sache toujours dans quel vault il opère
- `kb:` = base de connaissance et réflexion ; `project:` = source primaire du projet Cowork App

### Prochaine étape

- Lancer `/ingest project` pour compiler les 18 notes existantes de `project:wiki/` dans `kb:`
- Peupler `project:wiki/Context/Projets.md` avec les détails réels du projet
